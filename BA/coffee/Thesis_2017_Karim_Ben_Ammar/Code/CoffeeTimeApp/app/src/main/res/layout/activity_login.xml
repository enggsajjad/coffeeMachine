<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/lightBraun"
    tools:context="ces.bachelor.coffeetime.Login">


    <Button
        android:text="Start scanning"
        android:textColor="@color/white"
        android:background="@drawable/button_custom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/scanButton"
        android:onClick="scan"
        android:paddingLeft="25dp"
        android:paddingRight="25dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="I see you new here\n I know it's coffee time but this won't take long \n Just follow the instructions on the pi screen, and press the scanning button whenever you ready"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/scanButton"
        android:layout_marginTop="30dp"
        android:id="@+id/textView3" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/title"
        android:text="Coffee Time"
        android:textAlignment="center"
        android:textColor="@color/braun"
        android:layout_marginTop="100dp"
        android:textStyle="normal|bold"
        android:textSize="36sp"
        android:fontFamily="sans-serif-condensed"
        android:textAllCaps="false" />

</RelativeLayout>